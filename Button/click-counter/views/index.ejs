<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>게시판</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    /* 페이지 전체 스타일 */
    body {
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      background-color: #f9f9f9;
    }

    h1 {
      text-align: center;
      margin-top: 20px;
      font-size: 24px;
      color: #333;
    }

    /* 게시글 카드의 공통 스타일 */
    .post {
      display: flex;
      justify-content: space-between;  /* 제목은 왼쪽, 날짜는 오른쪽 정렬 */
      align-items: center;
      background-color: white;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      margin: 20px 0;
      padding: 20px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      width: 90%;  /* 카드 크기를 90%로 설정 */
      max-width: 800px;  /* 최대 크기 설정 */
    }

    .post:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 15px rgba(0, 0, 0, 0.15);
    }

    /* 제목 스타일 */
    .post-title {
      font-size: 18px;
      font-weight: bold;
      color: #333;
      margin-bottom: 5px;
      text-decoration: none;
      width: 60%;  /* 제목 칸의 크기 */
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis; /* 너무 긴 제목은 '...'으로 표시 */
    }

    .post-title:hover {
      color: #007bff;
    }

    /* 날짜 스타일 */
    .post-date {
      font-size: 14px;
      color: #888;
      text-align: right;
      width: 30%;  /* 날짜 칸의 크기 */
    }

    /* 게시글 목록 스타일 */
    #post-list {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px;
    }

    /* 글 작성 버튼 스타일 */
    .create-post-btn {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background-color: #007bff;
      color: white;
      padding: 15px 30px;
      font-size: 16px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      transition: background-color 0.3s;
    }

    .create-post-btn:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>

  <h1>게시판</h1>

  <!-- 게시글 목록 -->
  <div id="post-list">
    <% posts.forEach(post => { %>
      <div class="post">
        <a href="/posts/<%= post.id %>" class="post-title"><%= post.title %></a>
        <div class="post-date"><%= post.date %></div>
      </div>
    <% }) %>
  </div>

  <!-- 글 작성 버튼 -->
  <a href="/create">
    <button class="create-post-btn">글 작성</button>
  </a>

</body>
</html>
